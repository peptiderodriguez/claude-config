!!! info "Single-source page"
    This runbook is included verbatim from `CLUSTER_INSTALL.md` at the repo root, so it never drifts from the file Claude actually follows. It's written as instructions *to a cluster-side Claude session*, but reads fine as a human runbook.

--8<-- "CLUSTER_INSTALL.md"
